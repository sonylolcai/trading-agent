【文件定位】本文件职责：规范 Major Trend Reversal（MTR，主要趋势反转）的四组件、概率与阶段二决策。`detected_patterns` 含 `mtr` 或 `entry_setup_type=MTR` 时加载（叠加文件15）。

⚠️ MRV（Minor Trend Reversal，小趋势反转）≠ MTR：小反转更可能只是回撤或进入区间，不要过早标 `mtr`。

---

## 1. MTR 定义与概率

MTR 是**逆原主趋势**的可靠反转 attempt，目标形成新的 HH/HL 或 LL/LH 序列。

**概率锚点**：
- 第一次反转尝试成功率约 35–40%，多数失败变旗形。
- 第二次反转尝试成为主要反转的概率约 40%，**优先等待 H2/L2 或二次入场**。
- 即使图形完美，MTR 单笔胜率 rarely 超过 50%——须严格 §10.3。

---

## 2. MTR 四组件（缺一不可标 mtr）

1. **原趋势存在**：长程结构窗口有清晰 bull/bear trend（HH+HL 或 LL+LH）。
2. **趋势线/通道突破**：价格收盘突破主要趋势线或牛市/熊市通道线（非单根毛刺）。
3. **趋势恢复失败**：突破后原趋势方未能恢复（无强跟随、无新高/新低）。
4. **前极点测试失败**：价格测试前高/前低但无法创出超越性新高/新低（双顶底或更低高点/更高低点）。

**阶段一**：仅有单根反转棒 → `reversal_attempt`，**不要**写 `mtr`。
**阶段二**：四组件齐全且二次入场触发 → 可执行 MTR 方案。

---

## 3. MTR 与 H1/H2/L1/L2

| 背景 | 第一次信号 | 推荐 |
|------|------------|------|
| 强趋势 AIL/AIS | H1/L1 逆势 | 几乎总是等待 |
| 趋势线刚突破 | 第一次回撤入场 | 谨慎，偏 H2/L2 |
| 极点测试失败 | 第二次触发 | **H2/L2 或 MTR 二次入场** |
| 楔形三推后 | 第一次突破 | 等楔形突破 + 回撤 |

`bar_analysis.signal_bar.pattern`：MTR 场景优先 H2/L2；若用 H1/L1，reasoning 须逐条反驳「为何不等第二次」。

---

## 4. MTR vs 其他反转标签

| 标签 | 含义 | detected_patterns |
|------|------|-------------------|
| reversal_attempt | 有反转迹象，未完整 | reversal_attempt |
| mtr | 四组件基本齐全 | mtr + reversal_attempt |
| final_flag | 趋势末端旗形 | final_flag（见文件24） |
| wedge 反转 | 三推楔形 | wedge |
| MRV | 小级别反转，多半回撤 | 不写 mtr，写 reversal_attempt 或 none |

---

## 5. 入场、止损、目标

**入场**：
- 优先二次入场：极点测试失败后的回撤再突破。
- 信号棒：收盘近极点、实体饱满；铁丝网/区间中部信号无效。
- order_type：止损单或突破单；限价单须在结构位明确时。

**止损**：
- 信号棒极点外 1 跳动，或 MTR 结构失效位（前极点外侧）。
- MTR 止损往往较远——用 §10.2 过滤，过远则 wait。

**目标**：
- 按文件23：MM（原趋势 leg 翻测）、区间对边、通道对边。
- 第一目标常在 1R–1.5R 内的结构位，不追求远 MM 若方程不通过。

---

## 6. §14 与常见禁止

- 尖峰/微型通道中做 MTR 反转 → 禁止（除非 spike_ending + 四组件齐全）。
- 仅单根外包棒/锤子线 → 禁止标 MTR 并下单。
- Always In 强趋势中首个逆势 MTR → 默认不下单，除非 2.3 方向重判 + 双确认。
- FF 失败但无极点测试失败 → reversal_attempt，wait 二次。

---

## 7. 阶段二 JSON 映射

- `bar_analysis.entry_setup_type` = `MTR`
- `detected_patterns` 须含 `mtr` 与 `reversal_attempt`
- `decision_trace` §8 分支须覆盖 MTR 相关节点（按二元决策 §8）
- `diagnosis_summary.key_signals` 与阶段一 `key_signals` 一致引用四组件
- `terminal`：无二次确认 → wait @ 9.0；方程不通过 → reject @ 10.3
